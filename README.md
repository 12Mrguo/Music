# 旅游APP
```pre

├── build                    // webpack打包配置
├── config                   // webpack环境配置
├── dist                     // 运行build后生成目录
└── src                      // 生产目录(着重说明)
    ├── assets               // 静态资源(styles)
    ├── components           // vue组件
    ├── config               // Vue全局方法/过滤器
    ├── store                // vuex配置文件
    ├── main.js              // 项目入口文件
    └── router.js            // 路由配置


```